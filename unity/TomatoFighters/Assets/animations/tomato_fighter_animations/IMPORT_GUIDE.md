# tomato_fighter — Unity Import Guide

Generated by Animation Forge v0.1.0 on 2026-03-03 00:38 UTC

## Package Contents

| File | Description |
|------|-------------|
| `Sprites/tomato_fighter_idle.png` | idle spritesheet (50 frames) |
| `Sprites/tomato_fighter_walk.png` | walk spritesheet (50 frames) |
| `Sprites/tomato_fighter_run.png` | run spritesheet (50 frames) |
| `Animator/tomato_fighter_controller.json` | AnimatorController scaffold |
| `Animator/tomato_fighterAnimatorParams.cs` | C# parameter constants |
| `metadata.json` | Machine-readable export metadata |

## Import Steps

### 1. Copy Files
Copy the entire `tomato_fighter_animations/` directory into your Unity project under `Assets/Sprites/tomato_fighter/`.

### 2. Configure Sprite Import Settings
For each spritesheet PNG:
1. Select the file in Unity's Project window
2. Set **Texture Type** → Sprite (2D and UI)
3. Set **Sprite Mode** → Multiple
4. Set **Pixels Per Unit** → 256
5. Set **Filter Mode** → Point (no filter) for pixel art, Bilinear for HD
6. Open **Sprite Editor** and slice using grid:
   - Cell Size: 464 x 688
   - Pivot: Bottom Center
7. Click **Apply**

### 3. Create Animations
For each animation clip:
- **idle**: 50 frames at 12 fps (Loop Time: ON)
- **walk**: 50 frames at 14 fps (Loop Time: ON)
- **run**: 50 frames at 16 fps (Loop Time: ON)

### 4. Animator Controller
1. Open `tomato_fighter_AnimatorController` in the Animator window
2. Verify all states and transitions are connected
3. Assign sprite animation clips to each state

### 5. Parameters
The following Animator parameters are pre-configured:
| Parameter | Type | Default |
|-----------|------|---------|
| Speed | Float | 0.0 |
| IsGrounded | Bool | true |
| AttackTrigger | Trigger | — |
| HurtTrigger | Trigger | — |
| DeathTrigger | Trigger | — |

## Animation Reference

### Looping Animations
- **idle** (50 frames)
- **walk** (50 frames)
- **run** (50 frames)

## Troubleshooting

- **Sprites look blurry**: Set Filter Mode to Point (no filter) and disable compression
- **Animation too fast/slow**: Adjust Sample Rate in Animation window (default: 12 fps)
- **Transparent edges visible**: Ensure sprite mesh type is set to Tight, not Full Rect
